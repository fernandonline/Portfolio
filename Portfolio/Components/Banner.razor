<section class="banner-section d-flex flex-column min-vh-100 justify-content-center align-items-center position-relative overflow-hidden">

        <div class="d-flex flex-column text-start w-50 mb-5" style="z-index:1;">
            <span class="display-2 fw-bold">LUIZ</span>
            <span class="display-2 fw-bold">FERNANDO</span>
            <span class="display-2 fw-semibold">PORTFOLIO</span>
            <span>
                Construindo soluções completas da web ao mobile
            </span>

            <div>
                <a href="https://github.com/fernandonline" target="_blank">
                    <img src="assets/logo-github-svgrepo-com.svg" style="max-width:40px;" />
                </a>
                <a href="https://linkedin.com/in/fernandonline" target="_blank">
                    <img src="assets/linkedin-svgrepo-com.svg" style="max-width:50px;" />
                </a>
            </div>
        </div>

    <div class="light-band"></div>
    <div class="dark-horizon"></div>
</section>

<style>
    .banner-section {
        background: radial-gradient(ellipse at bottom, #1a0033 5%, #000 85%);
    }

    .light-band {
        position: absolute;
        bottom: -22%;
        left: 50%;
        width: 220%;
        height: 80%;
        background: radial-gradient( ellipse at center, rgba(255, 255, 255, 0.9) 0%, rgba(255, 255, 255, 0.2) 30%, rgba(255, 255, 255, 0) 70% );
        filter: blur(10px);
        animation: lightPulse 5s ease-in-out infinite alternate;
        opacity: 0.6;
    }

    @@keyframes lightPulse {
        0% {
            opacity: 0.1;
            transform: translateX(-50%) scale(1);
        }

        100% {
            opacity: 0.75;
            transform: translateX(-50%) scale(1.05);
        }
    }

    .dark-horizon {
        position: absolute;
        bottom: -38%;
        left: 50%;
        width: 135%;
        border-radius: 50%;
        height: 60%;
        transform: translateX(-50%);
        background: radial-gradient( ellipse at center, rgba(0, 0, 0, 1) 40%, rgba(0, 0, 0, 0.98) 60%, rgba(0, 0, 0, 0.5) 80% );
    }

    .dark-horizon::after {
        content: '';
        position: absolute;
        inset: 0;
        border-radius: 50%;
        background: radial-gradient( ellipse at center, rgba(255, 255, 255, 0) 0%, rgba(255, 255, 255, 0) 65%, rgba(255, 255, 255, 0.4) 75% );
        animation: horizonGlow 5s ease-in-out infinite alternate;
        pointer-events: none;
    }

    @@keyframes horizonGlow {
        0% {
            opacity: 0;
        }

        100% {
            opacity: 0.7;
        }
    }

    @@media (max-width: 768px) {
        .light-band {
            bottom: -25vw;
        }
        .dark-horizon {
            bottom: -35vw;
            width: 200%;
        }
    }

    @@media (max-width: 420px) {
        .light-band {
            bottom: -40vw;
        }
        .dark-horizon {
            bottom: -65vw;
            width: 200%;
        }
    }
</style>